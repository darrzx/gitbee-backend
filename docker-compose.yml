version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"  
    command: >
      sh -c "cd api && npx prisma generate && npm run dev"
    restart: always